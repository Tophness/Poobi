<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="160dp"
    android:layout_height="wrap_content"
    android:focusable="true"
    android:clickable="true"
    android:background="@drawable/bg_focusable"
    android:layout_margin="5dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="6dp">

        <ImageView
            android:id="@+id/card_thumb"
            android:layout_width="match_parent"
            android:layout_height="200dp"
            android:scaleType="centerCrop"
            android:background="#222222" />

        <!-- Fixed height container for all text to keep cards uniform and utilize space under title -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="100dp"
            android:orientation="vertical"
            android:layout_marginTop="6dp">

            <TextView
                android:id="@+id/card_title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="#FFFFFF"
                android:textSize="16sp"
                android:textStyle="bold"
                android:maxLines="2"
                android:ellipsize="end"
                android:text="Title" />

            <TextView
                android:id="@+id/card_episode"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="#40C4FF"
                android:textSize="12sp"
                android:textStyle="bold"
                android:visibility="gone"
                android:text="S1 E1" />

            <!-- Spacer to push Genres and Detail to the bottom of the 100dp area -->
            <Space
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="0" />

            <TextView
                android:id="@+id/card_detail"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="#888888"
                android:textSize="14sp"
                android:layout_marginTop="2dp"
                android:maxLines="1"
                android:ellipsize="end"
                android:text="Detail" />

            <TextView
                android:id="@+id/card_genres"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="#FFB74D"
                android:textSize="15sp"
                android:singleLine="true"
                android:ellipsize="marquee"
                android:marqueeRepeatLimit="marquee_forever"
                android:scrollHorizontally="true"
                android:text="Genres"
                android:visibility="gone" />
        </LinearLayout>
    </LinearLayout>

    <!-- Overlay Rating on top left -->
    <FrameLayout
        android:id="@+id/card_rating_container"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:layout_gravity="top|start"
        android:layout_marginTop="12dp"
        android:layout_marginStart="12dp"
        android:background="@drawable/bg_round_dark"
        android:alpha="0.9"
        android:visibility="gone">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:text="⭐"
            android:textSize="24sp"
            android:alpha="1.0" />

        <TextView
            android:id="@+id/card_rating"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:textColor="#40C4FF"
            android:textSize="10sp"
            android:textStyle="bold"
            android:text=""
            android:paddingTop="1dp"
            android:shadowColor="#000000"
            android:shadowDx="3"
            android:shadowDy="3"
            android:shadowRadius="3" />
    </FrameLayout>

    <!-- Overlay Icons on top right -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_gravity="top|end"
        android:layout_marginTop="12dp"
        android:layout_marginEnd="12dp">

        <ImageView
            android:id="@+id/card_new_episode_icon"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:src="@drawable/ic_add"
            app:tint="#FFC107"
            android:visibility="gone"
            android:background="@drawable/bg_round_dark"
            android:padding="4dp"
            android:layout_marginEnd="4dp"
            android:alpha="0.8"/>

        <ImageView
            android:id="@+id/card_fav_icon"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:src="@drawable/ic_heart_filled"
            app:tint="#40C4FF"
            android:visibility="gone"
            android:background="@drawable/bg_round_dark"
            android:padding="4dp"
            android:alpha="0.8"/>
    </LinearLayout>

</FrameLayout>
